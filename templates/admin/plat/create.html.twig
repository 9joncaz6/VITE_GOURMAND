{% extends 'admin/admin_base.html.twig' %}

{% block title %}Créer un plat{% endblock %}

{% block admin_body %}

<h1 class="page-title">Créer un plat</h1>

<div class="card" style="max-width: 600px; margin: auto;">

    {{ form_start(form) }}

    <div class="form-group mb-3">
        {{ form_label(form.nom) }}
        {{ form_widget(form.nom, { attr: { class: 'form-control' } }) }}
    </div>

    <div class="form-group mb-3">
        {{ form_label(form.type) }}
        {{ form_widget(form.type, { attr: { class: 'form-select' } }) }}
    </div>

    <button class="btn btn-primary mt-3">Créer</button>

    {{ form_end(form) }}

    <div class="mt-3 d-flex gap-2">

        {% if app.request.get('menu') %}
            <a href="{{ path('admin_menu_edit', { id: app.request.get('menu') }) }}" class="btn btn-secondary">
                Retour au menu
            </a>
        {% endif %}

        <a href="{{ path('app_plat_index') }}" class="btn btn-outline-secondary">
            Retour à la liste des plats
        </a>
    </div>

</div>

{% endblock %}
