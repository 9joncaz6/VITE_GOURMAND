{% extends 'base.html.twig' %}

{% block title %}Espace Administrateur{% endblock %}

{% block body %}
<h1 class="mb-4">Espace Administrateur</h1>

<div class="card p-4 shadow-sm">

    <h3 class="mb-3">Gestion</h3>

    <div class="d-flex flex-column gap-2">

        <a href="{{ path('admin_employe_list') }}" class="btn btn-primary">
            Gestion des employés
        </a>

        <a href="{{ path('admin_stats') }}" class="btn btn-outline-primary">
            Statistiques & Chiffre d'affaires
        </a>

        <a href="{{ path('employe_commandes_index') }}" class="btn btn-secondary">
    Espace Employé (Commandes)
</a>


    </div>

</div>
{% endblock %}
